<odoo>
    <record id="view_form_checkout_back_home" model="ir.ui.view">
        <field name="name">view.form.checkout.back.home</field>
        <field name="model">library.checkout.back.home</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" invisible="1"/>
                    <field name="stage_id"
                           widget="statusbar"
                           options="{'fold_field': 'fold'}"
                           />
                </header>
                <sheet>
                    <group>
                        <field name="name_seq"/>
                        <field name="card_id"/>
                        <field name="state_card"/>
                        <field name="user_id"/>
                        <field name="borrow_date"/>
                        <field name="gt_name"/>
                        <field name="start_date"/>
                        <field name="end_date"/>
                        <field name="book_limit_card"/>
                        <field name="limit_syllabus_card"/>
                        <field name="type_document"/>
                        <field name="book_id"/>
                        <field name="meta_book_id"/>
                        <field name="project_id"/>
                        <field name="meta_project_id"/>
                        <field name="status_document"/>
                        <field name="category_book"/>
                        <field name="price_doc"/>
                        <field name="currency_id"/>
                        <field name="price"/>
                        <field name="note"/>
                    </group>
                </sheet>
                <!-- mail mixin fields -->
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>
</odoo>